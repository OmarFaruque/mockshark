/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.ibb.co', 'i.postimg.cc','res.cloudinary.com' , 'cdn-icons-png.flaticon.com'], // Add all external image domains here
  },
  
};

export default nextConfig;
